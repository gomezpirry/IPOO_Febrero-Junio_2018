//-------------------------------------------------
//   Calificación: 0.0
//
//--------------------------------------------------

#include <conio.ah>
#include <iostream>

using namespace std;

/// Nunca invoca esta funcion
void transformador(int teclas[5], string * clave){
	
	
	for(int i=0;i= < 5; i++){
		
		switch(teclas[i])
		{case 72:
			string[i]="up";
			break;
			string[i]"down";
			break;
			string[i]"left";
			break;
			string[i]"right";
			break;
			default:
				char car= teclas [i];
				string letraString // Falta ; esto genera error
		}
	}
}

int main(){
	
	int caracater[5]={};
	int contador = 0;
	
	
	while(contador < 5){
		
		int caracter = getch(); // Caracter no esta definida
		if(caracter ==0 || carcater == 244)
		continue;
		
		
		caracter[conter] = caracter; /// conter no esta definida
		conducir++; /// Esta variable no esta definida
	}
 
}

